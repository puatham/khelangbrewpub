# Data schemas

These schemas keep external evidence, brewery calibration and live batch state separate. They are deliberately permissive in brewery-specific sub-objects so the data model can evolve without breaking the Skill.
